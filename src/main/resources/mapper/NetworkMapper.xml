<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lifuyi.dev_monitor.dao.NetworkMapper">


    <select id="getByIp" resultType="com.lifuyi.dev_monitor.model.network.Network">
        select * from network where network_ip=#{network_ip}
    </select>

    <insert id="addOrUpdateNetWork" parameterType="com.lifuyi.dev_monitor.model.network.Network">
        insert into network(network_id,network_type,network_code,network_name,network_ip,input_address,
        output_agreement,enterprise_id,update_time) values (#{network_id},#{network_type},#{network_code},#{network_name},
        #{network_ip},#{input_address},#{output_agreement},#{enterprise_id},CURRENT_TIMESTAMP)
        ON DUPLICATE KEY UPDATE
        network_type=#{network_type},network_code=#{network_code},network_name=#{network_name},
        network_ip=#{network_ip},input_address=#{input_address},output_agreement=#{output_agreement},enterprise_id=#{enterprise_id},
        update_time=CURRENT_TIMESTAMP
    </insert>


    <select id="getListByEntity" parameterType="com.lifuyi.dev_monitor.model.network.Network"
            resultType="com.lifuyi.dev_monitor.model.network.resp.NetworkResp">
        select network_id,network_type,network_code,network_name,network_ip,input_address,
        output_agreement,net.enterprise_id,
        (select enterprise_name from enterprise_info where enterprise_id=net.enterprise_id) as enterprise_name
        from network net where 1=1
        <if test="network_id!=null">
            and network_id=#{network_id}
        </if>
        <if test="network_name!=null">
            and network_name like concat('%',#{network_name},'%')
        </if>
        <if test="enterprise_id!=null">
            and enterprise_id=#{enterprise_id}
        </if>
         order by update_time desc
    </select>

    <delete id="deleteByNetWorkId">
        delete from network where network_id=#{network_id}
    </delete>

</mapper>